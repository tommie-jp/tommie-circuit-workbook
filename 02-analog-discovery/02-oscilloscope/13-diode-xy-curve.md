---
book: analog-discovery
chapter: 2
id: 2-13
title: ダイオードの順方向特性を XY で描く (Tracer の前段)
tier: 100
source: 自作
board: BB
---

# 2-13 ダイオードの順方向特性を XY で描く (Tracer の前段)

2-5 の XY 表示を使うと、電圧をゆっくり掃引しながら電流を同時に読むだけで
**部品の V-I 特性そのもの**が画面に描ける。ここではダイオードで試す。
AD3 の Tracer（9-6）はこれを自動でやってくれる専用の計器だが、仕組みは
今回の「三角波で掃引 + 電流検出抵抗」と同じ。

## 回路図

```circuit
title: 図1 ダイオードの順方向特性を掃引する
parts:
  W1: triangle a1 c1 1.5
  R1: resistor a1 a3 330
  D1: diode a3 a5
  Rs: resistor a5 a7 22
  G1: ground a7
  M1: voltmeter e3 e7 l=$\mathrm{CH1}$
  M2: voltmeter g5 g7 l=$\mathrm{CH2}$
wires:
  - a3 -- e3
  - a5 -- g5
  - a7 -- e7
  - a7 -- g7
  - c1 -- a7
```

W1（三角波、1.5 V の振幅・1.5 V のオフセットで 0〜3 V を往復）→ R1（330 Ω、電流
制限）→ D1（1N4148）→ Rs（22 Ω、電流検出）→ GND。CH1 は R1 の**先**（D1 と Rs を
合わせた両端＝ほぼダイオードの順電圧 V<sub>D</sub>）、CH2 は Rs の両端だけ
（電流を電圧で表したもの）。

## 実体配線図

```breadboard
title: 図2 ブレッドボードでダイオードの特性を測る
board: half
parts:
  R1: resistor b8 b12 330
  D1: diode c12(A) c14(K) 1N4148
  Rs: resistor e14 e18 22
  AD:
    type: device
    at: top
    label: Analog Discovery
    pins: [W1, 1+, 2+, 1-, 2-, GND]
wires:
  - AD.W1 -- a8 yellow
  - AD.1+ -- a12 orange
  - AD.2+ -- a14 blue
  - a18 -- -t18 black
  - AD.1- -- -t19 black
  - AD.2- -- -t20 black
  - AD.GND -- -t22 black
```

CH1（`1+` / `1-`）は R1 の**先**（12 列＝D1 のアノード側）と GND の間——D1 と
Rs を合わせた電圧、すなわちダイオードの順電圧にほぼ等しい（Rs は小さいため）。
CH2（`2+` / `2-`）は Rs の両端（14 列と GND）だけで、電流を mV の値として読む
（2-4 と同じ考え方）。

## 計器の設定

| 計器 | 設定 |
| --- | --- |
| Wavegen (W1) | 三角波、10 Hz（ゆっくり）、振幅 1.5 V、オフセット 1.5 V（0〜3 V） |
| Scope (CH1) | DC、Range 0〜1 V 程度 |
| Scope (CH2) | DC、Range 0〜200 mV 程度 |
| 表示 | XY モード。X = CH1（ダイオード電圧）、Y = CH2（電流 × 22 Ω） |

## 見るべき値

シリコンダイオードは V<sub>D</sub> ≈ 0.6〜0.7 V あたりから急に電流が増える
「ひざ」を持つ。ピーク付近（W1 が 3 V のとき）で見積もると：

I<sub>peak</sub> ≈ (3 V − 0.7 V) / (330 Ω + 22 Ω) ≈ **6.5 mA（計算値・近似）**

| V<sub>D</sub>（X 軸） | 見え方 | 分かること |
| --- | --- | --- |
| 0〜0.4 V | ほぼ 0（Y 軸に張り付いた横線） | この範囲ではダイオードはほとんど電流を流さない |
| 0.5〜0.6 V | わずかに立ち上がり始める | 電流が指数関数的に増え始める領域 |
| 0.65〜0.7 V | 急に立ち上がる「ひざ」 | シリコンダイオードの順方向特性の目印 |
| ピーク（V<sub>D</sub> ≈ 0.7 V 付近） | Y 軸（CH2）が 22 Ω × 6.5 mA ≈ 143 mV 付近まで達する（計算値） | 22 Ω は 330 Ω に対して小さいので、掃引そのものをほとんど乱さない |

三角波を使うのは、電圧が単調に増えて減るので同じ曲線を**上りと下りの 2 回**
描き、重なることでダイオードに目立った容量成分（遅れ）が無いことも確かめられる
ため。

## 出典

自作。
