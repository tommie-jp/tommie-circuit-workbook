---
book: circuits
chapter: 8
id: 8-6
title: 人感 (PIR モジュール)
tier: 100
source: 自作
era: 今
---

# 8-6 人感 (PIR モジュール)

人の体から出る赤外線 (熱) の変化を検知する PIR (焦電型赤外線) センサーは、
自分で回路を組むと感度調整が難しい部品だが、**HC-SR501 のようなモジュール**なら
VCC・GND・OUT の 3 本をつなぐだけで使える。8-5 の遮光検出と違い、
**物が動いたときの熱の変化だけ**に反応する。

## 回路図

```circuit
title: 図1 PIR モジュールでインジケータを点ける
parts:
  PIR:
    type: device
    at: c6
    label: HC-SR501
    pins: [VCC, GND, OUT]
  VCC: vcc a1
  G1: ground a2
  R1: resistor e8 e10 150
  D1: led e10 e12
  G2: ground e12
wires:
  - a1 |- PIR.VCC
  - a2 |- PIR.GND
  - PIR.OUT -| e8
style:
  grid: on
```

- PIR モジュールの OUT は、電源電圧によらず**3.3 V の TTL レベル**で出るのが
  代表的な仕様 (データシート値)。5 V で電源を取っても OUT は 3.3 V までしか
  上がらないので、そのまま LED を光らせられる
- OUT は検知すると H、検知していないと L になる (基板上のジャンパで
  「検知しっぱなしで再トリガ」か「1 回検知したら一定時間だけ」を選べる品が多い)
- 基板上のトリムポット 2 個で**感度**と**保持時間 (5 秒〜200 秒程度)** を
  調整できる。この図では配線しない (モジュールの中で完結している)
- OUT はマイコンの GPIO にもそのままつなげる (11 章)。この図では LED
  インジケータで代用する

## 部品

| 記号 | 部品 | 値 |
| --- | --- | --- |
| PIR | 人感センサーモジュール (HC-SR501) | 検知距離 約 7 m、検知角 約 120°、OUT 3.3 V TTL |
| R1 | 抵抗 | 150 Ω |
| D1 | LED (赤) | V<sub>F</sub> ≈ 2.0 V |
| — | 電源 | 5 V (USB や電池) |

## 見るべき値

計算値。

| 測る所 | 期待する値 | 分かること |
| --- | --- | --- |
| 検知していないときの OUT | 約 0 V | D1 消灯 |
| 検知したときの OUT | 約 3.3 V | R1 に (3.3 − 2.0) / 150 Ω ≈ 8.7 mA 流れ、D1 点灯 |
| 検知してから OUT が H を保つ時間 | 数秒〜数十秒 (基板のトリムポット次第) | 保持時間の調整範囲 |
| 電源を入れた直後 | 数十秒ほど OUT が不安定 (初期化時間) | センサーが周囲の熱に慣れるまでの立ち上がり時間。データシートに明記されている定番の注意点 |

## 出典

自作。
